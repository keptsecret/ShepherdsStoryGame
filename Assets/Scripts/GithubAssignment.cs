using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GithubAssignment : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Hi world");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
